<select name="${tag.name}" id="${tag.id}" onchange="${tag.reloadOnChangeString}" data-rawtype="${tag.inputComponent.rawClassType.name}" data-includeblank="${tag.inputComponent.includeBlank}" data-blanklabel="${tag.blankLabel}" ${tag.dynamicAttributesToString}>${tag.selectoneblankoption}${tag.inputComponent.selectItensString}</select>