# Database Configuration
#
# Next Framework auto-detects this file from the classpath (connection*.properties).
# The framework creates a DataSource bean and JdbcTemplate automatically.
#
# Properties: driver, url, username, password
#
# AUTO_SERVER=TRUE allows multiple processes to access the database simultaneously.

driver=org.h2.Driver
url=jdbc:h2:file:./data/erplite;AUTO_SERVER=TRUE;MODE=LEGACY
username=sa
password=
